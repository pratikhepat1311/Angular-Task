import { TempdirectiveDirective } from './tempdirective.directive';

describe('TempdirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TempdirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
