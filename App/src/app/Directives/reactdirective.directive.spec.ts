import { ReactdirectiveDirective } from './reactdirective.directive';

describe('ReactdirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ReactdirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
